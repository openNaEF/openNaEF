package naef.shell;

import lib38k.logger.Logger;
import tef.skelton.shell.SkeltonShellSession;
import tef.ui.shell.ShellConnection;

public class NaefShellSession extends SkeltonShellSession {

    public NaefShellSession(ShellConnection connection, Logger logger) {
        super(connection, logger);
    }
}
